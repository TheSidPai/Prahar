<?xml version="1.0" encoding="utf-8"?>
<!--
  Today widget provider. Wider default (4x2) so both blocks and the progress
  bar sit at their natural size. Resizable down to 3x2 for smaller launchers.
-->
<appwidget-provider
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="240dp"
    android:minHeight="150dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="120dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_today"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_today"
    android:description="@string/today_widget_description"/>
