org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Note: android.builder.sdkDownload=false was tried here to stop AGP invoking
# sdkmanager for the NDK. It had no effect in this AGP version — the build
# failed identically — so it has been removed rather than left in place
# implying it does something. The NDK is installed explicitly instead; see
# CLAUDE.md.
