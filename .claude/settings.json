{
  "//": "Shared, tracked in git — so nothing machine-specific and no standing permission grants belong here. The bypassPermissions mode lives in settings.local.json, which is gitignored, because it should not travel to a clone. Note that PowerShell command rules were proven not to match anything on the development machine (see CLAUDE.md); do not spend a session adding more.",
  "permissions": {
    "allow": [
      "Read(//c/Users/TheSidPai/AppData/Local/Temp/claude/**)",
      "Write(//c/Users/TheSidPai/Prahar/build/commit-msg.txt)"
    ]
  }
}
